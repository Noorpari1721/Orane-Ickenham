"use client";   import BackToDashboard from "@/components/admin/BackToDashboard"; import Link from "next/link"; import { FormEvent, useEffect, useMemo, useState } from "react"; import {   ArrowLeft,   CheckCircle2,   Clock3,   Edit3,   Plus,   PoundSterling,   Power,   Search,   Sparkles,   Trash2,   X,   XCircle, } from "lucide-react";  type Service = {   id: string;   serviceNo: string;   name: string;   category: string;   duration: number;   price: number;   active: boolean; };  type ServiceForm = {   name: string;   category: string;   duration: string;   price: string; };  const emptyForm: ServiceForm = {   name: "",   category: "Premium Nails",   duration: "60",   price: "", };  function formatMoney(value: number) {   const pound = String.fromCharCode(163);   return `${pound}${Number(value || 0).toFixed(2)}`; }  function formatDuration(minutes: number) {   const value = Number(minutes || 0);    if (value < 60) {     return `${value} min`;   }    const hours = Math.floor(value / 60);   const remaining = value % 60;    if (remaining === 0) {     return `${hours} hr`;   }    return `${hours} hr ${remaining} min`; }  export default function AdminServicesPage() {   const [services, setServices] = useState<Service[]>([]);   const [search, setSearch] = useState("");   const [category, setCategory] = useState("All");    const [loading, setLoading] = useState(true);   const [saving, setSaving] = useState(false);   const [error, setError] = useState("");    const [showModal, setShowModal] = useState(false);   const [editingService, setEditingService] =     useState<Service | null>(null);    const [form, setForm] = useState<ServiceForm>(emptyForm);    async function loadServices() {     try {       setLoading(true);       setError("");        const response = await fetch(         "/api/admin/services",         {           cache: "no-store",         }       );        const data = await response.json();        if (!response.ok) {         throw new Error(           data.error || "Unable to load services."         );       }        setServices(data.services || []);     } catch (err) {       setError(         err instanceof Error           ? err.message           : "Unable to load services."       );     } finally {       setLoading(false);     }   }    /* eslint-disable react-hooks/set-state-in-effect */   useEffect(() => {     loadServices();   }, []);   /* eslint-enable react-hooks/set-state-in-effect */    const categories = useMemo(() => {     const values = services       .map((service) => service.category)       .filter(Boolean);      return [       "All",       ...Array.from(new Set(values)),     ];   }, [services]);    const filteredServices = useMemo(() => {     const query = search.trim().toLowerCase();      return services.filter((service) => {       const matchesCategory =         category === "All" ||         service.category === category;        const matchesSearch =         !query ||         service.name           .toLowerCase()           .includes(query) ||         service.category           .toLowerCase()           .includes(query) ||         service.serviceNo           .toLowerCase()           .includes(query);        return matchesCategory && matchesSearch;     });   }, [services, search, category]);    const activeServices = services.filter(     (service) => service.active   ).length;    const inactiveServices =     services.length - activeServices;    const averagePrice =     services.length > 0       ? services.reduce(           (total, service) =>             total + Number(service.price || 0),           0         ) / services.length       : 0;    const categoriesCount = new Set(     services.map((service) => service.category)   ).size;    function openAddModal() {     setEditingService(null);     setForm(emptyForm);     setError("");     setShowModal(true);   }    function openEditModal(service: Service) {     setEditingService(service);      setForm({       name: service.name,       category: service.category,       duration: String(service.duration),       price: String(service.price),     });      setError("");     setShowModal(true);   }    function closeModal() {     if (saving) return;      setShowModal(false);     setEditingService(null);     setForm(emptyForm);   }    async function handleSubmit(     event: FormEvent<HTMLFormElement>   ) {     event.preventDefault();      if (!form.name.trim()) {       setError("Please enter a service name.");       return;     }      if (!form.category.trim()) {       setError("Please enter a category.");       return;     }      const duration = Number(form.duration);     const price = Number(form.price);      if (!Number.isFinite(duration) || duration <= 0) {       setError("Please enter a valid duration.");       return;     }      if (!Number.isFinite(price) || price < 0) {       setError("Please enter a valid price.");       return;     }      try {       setSaving(true);       setError("");        const isEditing =         Boolean(editingService);        const response = await fetch(         "/api/admin/services",         {           method: isEditing ? "PUT" : "POST",           headers: {             "Content-Type": "application/json",           },           body: JSON.stringify({             ...(isEditing               ? { id: editingService?.id }               : {}),             name: form.name.trim(),             category: form.category.trim(),             duration,             price,           }),         }       );        const data = await response.json();        if (!response.ok) {         throw new Error(           data.error || "Unable to save service."         );       }        if (isEditing) {         setServices((current) =>           current.map((service) =>             service.id === data.service.id               ? data.service               : service           )         );       } else {         setServices((current) => [           data.service,           ...current,         ]);       }        closeModal();     } catch (err) {       setError(         err instanceof Error           ? err.message           : "Unable to save service."       );     } finally {       setSaving(false);     }   }    async function toggleActive(service: Service) {     try {       setError("");        const response = await fetch(         "/api/admin/services",         {           method: "PATCH",           headers: {             "Content-Type": "application/json",           },           body: JSON.stringify({             id: service.id,             active: !service.active,           }),         }       );        const data = await response.json();        if (!response.ok) {         throw new Error(           data.error ||             "Unable to change service status."         );       }        setServices((current) =>         current.map((item) =>           item.id === data.service.id             ? data.service             : item         )       );     } catch (err) {       setError(         err instanceof Error           ? err.message           : "Unable to change service status."       );     }   }    async function deleteService(     service: Service   ) {     const confirmed = window.confirm(       "Delete " +         service.name +         " permanently?"     );      if (!confirmed) return;      try {       setError("");        const response = await fetch(         "/api/admin/services",         {           method: "DELETE",           headers: {             "Content-Type": "application/json",           },           body: JSON.stringify({             id: service.id,           }),         }       );        const data = await response.json();        if (!response.ok) {         throw new Error(           data.error ||             "Unable to delete service."         );       }        setServices((current) =>         current.filter(           (item) => item.id !== service.id         )       );     } catch (err) {       setError(         err instanceof Error           ? err.message           : "Unable to delete service."       );     }   }    return (     <main className="min-h-screen bg-[#090909] text-white">       <div className="p-5 sm:p-8 lg:p-10">         <div className="mx-auto max-w-[1600px]">            <BackToDashboard />             <header className="mb-8 flex flex-col gap-5 md:flex-row md:items-end md:justify-between">             <div>                <p className="text-xs uppercase tracking-[0.3em] text-[#D4AF37]">                 Management               </p>                <h1 className="mt-2 text-3xl font-light sm:text-4xl">                 Services               </h1>                <p className="mt-2 text-sm text-white/50">                 Manage salon treatments, pricing and durations.               </p>             </div>              <button               type="button"               onClick={openAddModal}               className="inline-flex w-fit items-center gap-2 rounded-xl bg-[#D4AF37] px-5 py-3 text-sm font-medium text-black transition hover:bg-[#e0bd45]"             >               <Plus size={17} />               Add Service             </button>           </header>            {error && (             <div className="mb-6 rounded-2xl border border-red-400/20 bg-red-400/5 p-4 text-sm text-red-300">               {error}             </div>           )}            <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">             <StatCard               label="Total Services"               value={                 loading                   ? "..."                   : String(services.length)               }               description="Services in catalogue"               icon={Sparkles}             />              <StatCard               label="Active"               value={                 loading                   ? "..."                   : String(activeServices)               }               description="Available for booking"               icon={CheckCircle2}             />              <StatCard               label="Categories"               value={                 loading                   ? "..."                   : String(categoriesCount)               }               description="Service categories"               icon={Sparkles}             />              <StatCard               label="Average Price"               value={                 loading                   ? "..."                   : formatMoney(averagePrice)               }               description="Average service price"               icon={PoundSterling}             />           </div>            <section className="mt-8 rounded-2xl border border-white/10 bg-white/[0.03] p-4 sm:p-5">             <div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">                <div className="relative min-w-0 flex-1">                 <Search                   size={17}                   className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-white/25"                 />                  <input                   type="text"                   value={search}                   onChange={(event) =>                     setSearch(event.target.value)                   }                   placeholder="Search services or service numbers..."                   className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-11 pr-4 text-sm text-white outline-none placeholder:text-white/25 transition focus:border-[#D4AF37]/30"                 />               </div>                <div className="flex flex-wrap gap-2">                 {categories.map(                   (item) => (                     <button                       key={item}                       type="button"                       onClick={() =>                         setCategory(item)                       }                       className={`rounded-lg border px-3 py-2 text-xs transition ${                         category === item                           ? "border-[#D4AF37]/30 bg-[#D4AF37]/10 text-[#D4AF37]"                           : "border-white/10 bg-black/10 text-white/40 hover:text-white"                       }`}                     >                       {item}                     </button>                   )                 )}               </div>             </div>           </section>            <section className="mt-6 overflow-hidden rounded-2xl border border-white/10 bg-white/[0.03]">              <div className="border-b border-white/10 p-5 sm:p-6">               <p className="text-xs uppercase tracking-[0.25em] text-[#D4AF37]">                 Service Catalogue               </p>                <div className="mt-1 flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">                 <h2 className="text-xl font-light">                   All Services                 </h2>                  <p className="text-xs text-white/30">                   {filteredServices.length}{" "}                   {filteredServices.length === 1                     ? "service"                     : "services"}                 </p>               </div>             </div>              {loading ? (               <div className="flex min-h-[300px] items-center justify-center">                 <p className="text-sm text-white/40">                   Loading services...                 </p>               </div>             ) : filteredServices.length === 0 ? (               <div className="p-12 text-center">                 <Sparkles                   size={38}                   className="mx-auto text-white/15"                 />                  <p className="mt-4 text-sm text-white/45">                   {services.length === 0                     ? "No services found."                     : "No services match your filters."}                 </p>                  {services.length === 0 && (                   <button                     type="button"                     onClick={openAddModal}                     className="mt-5 inline-flex items-center gap-2 rounded-xl bg-[#D4AF37] px-4 py-2.5 text-sm text-black"                   >                     <Plus size={16} />                     Add First Service                   </button>                 )}               </div>             ) : (               <>                 <div className="hidden overflow-x-auto lg:block">                   <table className="w-full min-w-[900px]">                     <thead>                       <tr className="border-b border-white/10 text-left">                         <th className="px-6 py-4 text-[10px] uppercase tracking-[0.18em] text-white/25">                           Service                         </th>                          <th className="px-6 py-4 text-[10px] uppercase tracking-[0.18em] text-white/25">                           Category                         </th>                          <th className="px-6 py-4 text-[10px] uppercase tracking-[0.18em] text-white/25">                           Duration                         </th>                          <th className="px-6 py-4 text-[10px] uppercase tracking-[0.18em] text-white/25">                           Price                         </th>                          <th className="px-6 py-4 text-[10px] uppercase tracking-[0.18em] text-white/25">                           Status                         </th>                          <th className="px-6 py-4 text-right text-[10px] uppercase tracking-[0.18em] text-white/25">                           Actions                         </th>                       </tr>                     </thead>                      <tbody className="divide-y divide-white/10">                       {filteredServices.map(                         (service) => (                           <tr                             key={service.id}                             className="transition hover:bg-white/[0.025]"                           >                             <td className="px-6 py-5">                               <div className="flex items-center gap-3">                                 <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-[#D4AF37]/20 bg-[#D4AF37]/10">                                   <Sparkles                                     size={17}                                     className="text-[#D4AF37]"                                   />                                 </div>                                  <div>                                   <p className="text-sm text-white">                                     {service.name}                                   </p>                                    <p className="mt-1 text-[11px] text-white/25">                                     {service.serviceNo}                                   </p>                                 </div>                               </div>                             </td>                              <td className="px-6 py-5">                               <span className="rounded-full border border-white/10 bg-white/[0.03] px-3 py-1.5 text-xs text-white/50">                                 {service.category}                               </span>                             </td>                              <td className="px-6 py-5">                               <div className="flex items-center gap-2 text-sm text-white/60">                                 <Clock3                                   size={14}                                   className="text-white/25"                                 />                                 {formatDuration(                                   service.duration                                 )}                               </div>                             </td>                              <td className="px-6 py-5">                               <p className="text-sm">                                 {formatMoney(                                   service.price                                 )}                               </p>                             </td>                              <td className="px-6 py-5">                               <span                                 className={`inline-flex items-center gap-1.5 rounded-full border px-3 py-1.5 text-[11px] ${                                   service.active                                     ? "border-emerald-400/20 bg-emerald-400/10 text-emerald-300"                                     : "border-white/10 bg-white/5 text-white/40"                                 }`}                               >                                 {service.active ? (                                   <CheckCircle2                                     size={12}                                   />                                 ) : (                                   <XCircle size={12} />                                 )}                                  {service.active                                   ? "ACTIVE"                                   : "INACTIVE"}                               </span>                             </td>                              <td className="px-6 py-5">                               <div className="flex justify-end gap-2">                                 <button                                   type="button"                                   onClick={() =>                                     openEditModal(                                       service                                     )                                   }                                   title="Edit service"                                   className="rounded-lg border border-white/10 p-2 text-white/40 transition hover:border-[#D4AF37]/20 hover:text-[#D4AF37]"                                 >                                   <Edit3 size={15} />                                 </button>                                  <button                                   type="button"                                   onClick={() =>                                     toggleActive(                                       service                                     )                                   }                                   title={                                     service.active                                       ? "Deactivate service"                                       : "Activate service"                                   }                                   className="rounded-lg border border-white/10 p-2 text-white/40 transition hover:border-white/20 hover:text-white"                                 >                                   <Power size={15} />                                 </button>                                  <button                                   type="button"                                   onClick={() =>                                     deleteService(                                       service                                     )                                   }                                   title="Delete service"                                   className="rounded-lg border border-white/10 p-2 text-white/40 transition hover:border-red-400/20 hover:text-red-300"                                 >                                   <Trash2 size={15} />                                 </button>                               </div>                             </td>                           </tr>                         )                       )}                     </tbody>                   </table>                 </div>                  <div className="divide-y divide-white/10 lg:hidden">                   {filteredServices.map(                     (service) => (                       <div                         key={service.id}                         className="p-5"                       >                         <div className="flex items-start justify-between gap-4">                           <div className="flex min-w-0 items-center gap-3">                             <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-[#D4AF37]/20 bg-[#D4AF37]/10">                               <Sparkles                                 size={17}                                 className="text-[#D4AF37]"                               />                             </div>                              <div className="min-w-0">                               <p className="truncate text-sm">                                 {service.name}                               </p>                                <p className="mt-1 text-xs text-white/30">                                 {service.serviceNo}                               </p>                             </div>                           </div>                            <span                             className={`shrink-0 rounded-full border px-2.5 py-1 text-[10px] ${                               service.active                                 ? "border-emerald-400/20 bg-emerald-400/10 text-emerald-300"                                 : "border-white/10 bg-white/5 text-white/40"                             }`}                           >                             {service.active                               ? "ACTIVE"                               : "INACTIVE"}                           </span>                         </div>                          <div className="mt-5 grid grid-cols-2 gap-4">                           <InfoItem                             label="Category"                             value={service.category}                           />                            <InfoItem                             label="Duration"                             value={formatDuration(                               service.duration                             )}                           />                            <InfoItem                             label="Price"                             value={formatMoney(                               service.price                             )}                           />                         </div>                          <div className="mt-5 flex gap-2">                           <button                             type="button"                             onClick={() =>                               openEditModal(                                 service                               )                             }                             className="flex-1 rounded-xl border border-white/10 py-2.5 text-xs text-white/50"                           >                             Edit                           </button>                            <button                             type="button"                             onClick={() =>                               toggleActive(                                 service                               )                             }                             className="flex-1 rounded-xl border border-white/10 py-2.5 text-xs text-white/50"                           >                             {service.active                               ? "Deactivate"                               : "Activate"}                           </button>                            <button                             type="button"                             onClick={() =>                               deleteService(                                 service                               )                             }                             className="rounded-xl border border-red-400/10 px-4 py-2.5 text-xs text-red-300/70"                           >                             <Trash2 size={15} />                           </button>                         </div>                       </div>                     )                   )}                 </div>               </>             )}           </section>            <div className="mt-6 grid gap-4 sm:grid-cols-2">             <div className="rounded-2xl border border-white/10 bg-white/[0.02] p-5">               <p className="text-xs uppercase tracking-[0.2em] text-white/25">                 Active Services               </p>                <p className="mt-3 text-2xl font-light">                 {loading                   ? "..."                   : activeServices}               </p>                <p className="mt-1 text-xs text-white/30">                 {inactiveServices} inactive services               </p>             </div>              <div className="rounded-2xl border border-white/10 bg-white/[0.02] p-5">               <p className="text-xs uppercase tracking-[0.2em] text-white/25">                 Catalogue               </p>                <p className="mt-3 text-2xl font-light">                 {loading                   ? "..."                   : services.length}               </p>                <p className="mt-1 text-xs text-white/30">                 Total services across{" "}                 {categoriesCount} categories               </p>             </div>           </div>          </div>       </div>        {showModal && (         <div           className="fixed inset-0 z-50 flex items-center justify-center overflow-y-auto bg-black/75 p-5 backdrop-blur-sm"           onMouseDown={(event) => {             if (               event.target === event.currentTarget &&               !saving             ) {               closeModal();             }           }}         >           <div className="my-8 w-full max-w-xl rounded-2xl border border-white/10 bg-[#111111] shadow-2xl">              <div className="flex items-center justify-between border-b border-white/10 p-6">               <div>                 <p className="text-xs uppercase tracking-[0.25em] text-[#D4AF37]">                   Service                 </p>                  <h2 className="mt-1 text-xl font-light">                   {editingService                     ? "Edit Service"                     : "Add Service"}                 </h2>               </div>                <button                 type="button"                 onClick={closeModal}                 disabled={saving}                 className="rounded-lg p-2 text-white/30 transition hover:bg-white/5 hover:text-white disabled:opacity-30"               >                 <X size={18} />               </button>             </div>              <form               onSubmit={handleSubmit}               className="p-6"             >               <div className="space-y-5">                 <Field                   label="Service Name"                   value={form.name}                   onChange={(value) =>                     setForm((current) => ({                       ...current,                       name: value,                     }))                   }                   required                 />                  <div>                   <label className="text-xs text-white/40">                     Category                     <span className="ml-1 text-[#D4AF37]">                       *                     </span>                   </label>                    <input                     type="text"                     value={form.category}                     onChange={(event) =>                       setForm((current) => ({                         ...current,                         category:                           event.target.value,                       }))                     }                     placeholder="e.g. Premium Nails"                     required                     className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-sm text-white outline-none placeholder:text-white/20 focus:border-[#D4AF37]/30"                   />                 </div>                  <div className="grid gap-5 sm:grid-cols-2">                   <Field                     label="Duration (minutes)"                     type="number"                     value={form.duration}                     onChange={(value) =>                       setForm((current) => ({                         ...current,                         duration: value,                       }))                     }                     required                   />                    <Field                     label="Price"                     type="number"                     value={form.price}                     onChange={(value) =>                       setForm((current) => ({                         ...current,                         price: value,                       }))                     }                     required                   />                 </div>               </div>                {error && (                 <div className="mt-5 rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-xs text-red-300">                   {error}                 </div>               )}                <div className="mt-6 flex gap-3">                 <button                   type="button"                   onClick={closeModal}                   disabled={saving}                   className="flex-1 rounded-xl border border-white/10 py-3 text-sm text-white/50 transition hover:bg-white/5 hover:text-white disabled:opacity-30"                 >                   Cancel                 </button>                  <button                   type="submit"                   disabled={saving}                   className="flex-1 rounded-xl bg-[#D4AF37] py-3 text-sm font-medium text-black transition hover:bg-[#e0bd45] disabled:opacity-50"                 >                   {saving                     ? "Saving..."                     : editingService                       ? "Save Changes"                       : "Add Service"}                 </button>               </div>             </form>           </div>         </div>       )}     </main>   ); }  function StatCard({   label,   value,   description,   icon: Icon, }: {   label: string;   value: string;   description: string;   icon: typeof Sparkles; }) {   return (     <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 transition hover:border-[#D4AF37]/20 hover:bg-white/[0.055]">       <div className="flex items-center justify-between gap-3">         <span className="text-sm text-white/50">           {label}         </span>          <Icon           size={18}           className="shrink-0 text-[#D4AF37]"         />       </div>        <p className="mt-4 text-3xl font-light">         {value}       </p>        <p className="mt-2 text-xs text-white/30">         {description}       </p>     </div>   ); }  function InfoItem({   label,   value, }: {   label: string;   value: string; }) {   return (     <div className="min-w-0">       <p className="text-[10px] uppercase tracking-[0.15em] text-white/25">         {label}       </p>        <p className="mt-1 truncate text-sm text-white/65">         {value}       </p>     </div>   ); }  function Field({   label,   value,   onChange,   type = "text",   required = false, }: {   label: string;   value: string;   onChange: (value: string) => void;   type?: string;   required?: boolean; }) {   return (     <div>       <label className="text-xs text-white/40">         {label}          {required && (           <span className="ml-1 text-[#D4AF37]">             *           </span>         )}       </label>        <input         type={type}         value={value}         onChange={(event) =>           onChange(event.target.value)         }         required={required}         min={           type === "number"             ? "0"             : undefined         }         className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 text-sm text-white outline-none placeholder:text-white/20 focus:border-[#D4AF37]/30"       />     </div>   ); }